# Production Environment Variables Example
# Copy this file to .env and update with your secure values

# Airflow Project Configuration
AIRFLOW_PROJ_DIR=.
AIRFLOW_UID=50000

# Environment Configuration
ENVIRONMENT=production  # development
DEBUG=false

# Airflow Admin User Configuration
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# Airflow Security Keys (CRITICAL - Generate new keys for each environment)
# Generate Keys Commands:
    # Fernet Key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
    # Secret Key: python -c "import secrets; print(secrets.token_hex(32))"
AIRFLOW__CORE__FERNET_KEY=your_fernet_key_here
AIRFLOW__API__SECRET_KEY=your_webserver_secret_key_here


# Security Note: Change all default passwords before deployment
# Use strong passwords with mix of letters, numbers, and symbols
# IMPORTANT: Backup your keys - losing them makes encrypted data unrecoverable
